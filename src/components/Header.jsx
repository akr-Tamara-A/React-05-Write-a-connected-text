import React from 'react';
import styled from 'styled-components';


function Header() {
  return (
    <HeaderWrapper>
      <HeaderTitle>Напиши связный текст</HeaderTitle>
    </HeaderWrapper>
  )
};

export default Header;

const HeaderWrapper = styled.header`

`;

const HeaderTitle = styled.h1`

`;
